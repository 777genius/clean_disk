name: clean_disk_cache
description: Drift-backed cache infrastructure for Clean Disk.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  drift: ^2.33.0
  drift_flutter: ^0.3.0
  flutter:
    sdk: flutter
  meta: ^1.17.0

dev_dependencies:
  build_runner: 2.13.1
  drift_dev: ^2.33.0
  fast_build_runner: ^0.1.6
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
