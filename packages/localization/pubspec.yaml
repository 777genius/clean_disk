name: clean_disk_localization
description: Shared localization package for Clean Disk Flutter UI.
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

flutter:
  generate: true

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
